{
  "name": "zuvo-app",
  "version": "1.0.0",
  "description": "Zuvo — comparador bancario + seguimiento de cartera con login, base de datos y precios en vivo (Twelve Data)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "local": "node --env-file=.env server.js",
    "dev": "node --watch --env-file=.env server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cookie-parser": "^1.4.6",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  }
}
